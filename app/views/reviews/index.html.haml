%h1 Listing reviews

%table
  %thead
    %tr
      %th Rate
      %th Comment
      %th User
      %th Beer
      %th
      %th
      %th

  %tbody
    - @reviews.each do |review|
      %tr
        %td= review.rate
        %td= review.comment
        %td= link_to review.user.name, review.user
        %td= link_to review.beer.name, review.beer
        %td= review.created_at.strftime('%F')
        %td= link_to 'Show', review

%br
